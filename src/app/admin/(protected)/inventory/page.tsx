import { AdminModulePlaceholder } from "@/components/admin-module-placeholder";

export default function InventoryPage() {
  return (
    <AdminModulePlaceholder
      description="A focused home for shop products and the stock visibility the team will need as the system grows."
      eyebrow="Inventory"
      icon="inventory"
      nextPhase="Connect the product catalog and stock records so this workspace can reflect the shop floor."
      title="Keep the shop floor supplied."
    />
  );
}
