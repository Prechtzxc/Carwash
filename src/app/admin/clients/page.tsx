import { AdminModulePlaceholder } from "@/components/admin-module-placeholder";

export default function ClientsPage() {
  return (
    <AdminModulePlaceholder
      description="A focused home for customer records and the context that helps the team recognize returning clients."
      eyebrow="Clients"
      icon="clients"
      nextPhase="Connect customer records from the check-in flow so the client workspace can become useful."
      title="Know the people behind every visit."
    />
  );
}
